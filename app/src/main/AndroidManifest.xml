<?xml version="1.0" encoding="utf-8"?>
<manifest
    package="nearby.google.trifork.googlenearby"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="com.google.android.nearby.messages.API_KEY"
            android:value="AIzaSyDyNLn9kIVfWehzA9j_217ZdkZBCBBDubU"/>

        <meta-data
            android:name="com.google.android.gms.nearby.connection.SERVICE_ID"
            android:value="nearby.google.trifork.googlenearby"/>

        <activity android:name=".HostActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".NearbyMessagesActivity"/>
        <activity android:name=".NearbyConnectionsActivity"/>

    </application>

</manifest>